version: '3.8'

services:
  homeassistant:
    build:
      context: .
      dockerfile: .docker/Dockerfile
    container_name: homeassistant
    restart: unless-stopped
    network_mode: bridge
    volumes:
      - ./config:/config
      - ./my-lovelace-card:/config/custom_components/my_lovelace_card
      - /etc/localtime:/etc/localtime:ro
    environment:
      - TZ=UTC
    # Explicitly set the project name to avoid conflicts
    # This ensures only this service is started
    # The project name is used as a prefix for container names
    # and helps isolate the environment
    # Use a unique project name to avoid conflicts with other projects
    # project: homeassistant-project
    # Use the --project-name flag when running docker compose to ensure this name is used
